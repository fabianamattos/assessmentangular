import { ApiInfnetService } from "./apiInfnet.service";
import { TestBed } from "@angular/core/testing";

describe("ApiInfnetService", () => {

  let service: ApiInfnetService;
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        ApiInfnetService
      ]
    });
    service = TestBed.get(ApiInfnetService);

  });

  it("should be able to create service instance", () => {
    expect(service).toBeDefined();
  });

});
